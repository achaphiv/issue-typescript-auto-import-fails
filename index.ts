import { ref } from "@vue/composition-api";

// Put cursor after `computed` and attempt to auto-import from `vue-demi`
// The presence of the `ref` import somehow breaks the `computed` import.

computed
